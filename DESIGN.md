# HX Personal Site — DESIGN.md

个人主页固定使用浅色视觉：白色主画布、蓝黑正文、冷蓝强调。不提供整站深色主题。

## 1. 颜色

```text
canvas        #ffffff
canvas-soft   #f6f8fb
surface       #ffffff
ink           #0c1728
ink-muted     #566579
ink-subtle    #8a96a6
hairline      #e4e9f0
hairline-2    #d3dbe6
accent        #2563eb
accent-hover  #1d4ed8
accent-soft   #edf4ff
success       #23966a
```

- 页面以白色为主，不使用大面积黑色背景。
- 蓝色只用于选中态、主按钮、链接和少量状态。
- 卡片主要靠 1px 边框和轻微阴影区分，不做霓虹、玻璃拟态和彩虹渐变。

## 2. 导航

桌面端使用固定左侧菜单栏，这是站点的主导航，不再用普通顶部链接代替。

菜单至少包含：

- 首页
- 项目
- 关于
- GitHub
- Amia · Mizuki 开发组
- Mizuki Docs

规则：

- 当前页面必须有明确选中态。
- 左侧栏固定，正文区域独立滚动页面。
- 小于 900px 时左侧栏变为侧滑抽屉。
- 手机顶部必须有清晰可见的菜单按钮。
- 菜单打开后有遮罩；点击遮罩、菜单链接或按 Esc 均可关闭。
- 项目详情页也使用同一套菜单，不能点进去以后失去站内导航。

## 3. 项目卡

项目卡不是 GitHub 仓库列表的复制品。

每张卡固定包含：

1. 给人看的项目标题；
2. 真实 GitHub repo 名，作为第二层信息；
3. 两段简短说明：第一段说明用途，第二段补充主要流程、当前状态或具体做法；
4. 3–4 个技术或关键词标签；
5. 快速看 / 详情 / GitHub；有正式网站时增加“打开网站”。

规则：

- 不把版本号、Release Tag 或内部代号当项目名。
- repo 名必须保持 GitHub 上的真实拼写。
- 不强制把 repo 名作为视觉主标题。
- 标签、说明和操作区必须自然换行，不能靠横向滚动解决。
- 手机端禁止出现可见横向滚动条。
- 卡片分隔线只允许出现在操作区上方，必须与文字和标签保持足够距离，不能出现“横线穿过内容”的视觉效果。

## 4. 项目页交互

- 搜索支持项目标题、repo 名、技术和简介。
- 分类：全部 / 无线电 / Bot·音游 / 工具 / 系统·硬件。
- 每页 6 个，筛选和搜索后重新计算分页。
- 搜索、分类、页码同步到 URL。
- 快速看使用 Modal；Esc 关闭，`/` 聚焦搜索。
- 手机分类按钮改为网格布局，不使用横向滚动条。

## 5. 文案

- 中文为主；英文只保留 repo 名、技术名和必要标签。
- 只写事实：这是干什么的、现在怎样、怎么做。
- 项目简介不能只有一句泛泛说明，要让第一次看到的人知道用途和基本流程。
- 不解释网站自己的设计决定。
- 避免“探索、构建、持续演进、数字空间、技术边界”等空泛表达。
- 呼号不作为个人身份标签；只有项目本身确实以呼号命名时才出现。

## 6. 响应式

- 桌面：固定 248px 左侧栏，正文居中。
- 平板 / 手机：64px 顶栏 + 侧滑菜单。
- 900px 以下项目卡单列。
- 620px 以下按钮、标签和卡片操作区优先换行，不允许页面产生横向溢出。
- `html` / `body` 禁止横向溢出。

## 7. 动效

- 菜单、按钮、卡片 hover：120–220ms。
- 页面进入动画约 450ms。
- 支持 `prefers-reduced-motion`，减少动画时直接显示内容。
